let arrayData = ["Paul", "John", "Ringo", "David Gimour"];

arrayData.pop("David Gimour"); //No matter what It removes last element //we will use index and slice for removing specific value

let arrayPopDataList = () => {
    return arrayData;
};

export default arrayPopDataList;